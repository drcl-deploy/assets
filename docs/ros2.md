# ROS 2 and sim-to-sim paths

Print the installed tracked asset root:

```bash
assets path
```

Print the per-machine generated root:

```bash
assets generated-path
```

Point ROS 2 configuration directly to files below the appropriate root. No
environment variable is required.
